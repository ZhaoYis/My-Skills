# Tasks: 炸弹系统

## 1. 数据结构与常量定义

- [ ] 1.1 新增 `BOMB_TYPES` 常量（DORMANT / UNSTABLE）
- [ ] 1.2 新增 `BOMB_CONFIG` 配置对象（生成间隔、上限、概率、引信范围、扣段范围、爆炸时长）
- [ ] 1.3 新增 `bombs[]` 和 `explosions[]` 状态变量
- [ ] 1.4 新增 `bombSpawnTimer` / `nextBombSpawnAt` 生成控制变量

## 2. 炸弹生成逻辑

- [ ] 2.1 实现 `spawnBomb()` 函数 — 随机空单元格、70/30 类型概率、预设引信时长
- [ ] 2.2 实现 `getCellsInRadius(x, y, radius)` 工具函数 — 返回范围内所有单元格坐标
- [ ] 2.3 在 `gameLoop()` 中集成炸弹生成计时器（生成间隔 4-6s 随机，上限 6 颗）
- [ ] 2.4 在 `initGame()` 中重置炸弹和爆炸状态

## 3. 爆炸与连锁反应

- [ ] 3.1 实现 `triggerExplosion(bomb, chainLevel)` — 3×3 范围检测、蛇头/蛇身/食物/炸弹分别处理
- [ ] 3.2 实现蛇身截断逻辑 — 从爆炸波及的最靠近蛇头段之后截断
- [ ] 3.3 实现连锁引爆递归 — 被波及炸弹自动调用 triggerExplosion 并递增 chainLevel
- [ ] 3.4 实现连锁倍率计分 — 被截断段按 chainLevel × 5 计分

## 4. 碰撞检测集成

- [ ] 4.1 在 `update()` 中蛇头移动到新位置后，先检测炸弹碰撞再检测食物碰撞
- [ ] 4.2 休眠炸弹触碰：扣 2-3 段（蛇过短则死亡），炸弹消失
- [ ] 4.3 不稳定炸弹触碰：立即调用 `triggerExplosion()`
- [ ] 4.4 不稳定炸弹自爆检测：在 `gameLoop()` 中检查引信是否到期

## 5. 护盾与炸弹交互

- [ ] 5.1 修改 `handleCollision()` — 爆炸死亡场景中护盾消耗（防死+方向反转），但不防休眠触碰扣段
- [ ] 5.2 护盾消耗后触发屏幕闪白反馈动画

## 6. 经典模式道具扩展

- [ ] 6.1 修改 `rollItemType()` — 经典模式也调用概率表生成特殊道具（金苹果/闪电/护盾）
- [ ] 6.2 经典模式下特殊道具效果适配：得分正常、不加时间
- [ ] 6.3 经典模式下闪电和护盾的视觉反馈与计时模式一致

## 7. 皮肤参数扩展

- [ ] 7.1 为 4 套皮肤的 `canvas` 对象各新增 9 个炸弹相关颜色参数
- [ ] 7.2 经典皮肤炸弹配色：暗红系炸弹 + 橙红爆炸
- [ ] 7.3 复古皮肤炸弹配色：灰黑系炸弹 + 亮橙爆炸
- [ ] 7.4 午夜皮肤炸弹配色：暗紫系炸弹 + 品红爆炸
- [ ] 7.5 暖橙皮肤炸弹配色：暗棕系炸弹 + 亮橙爆炸

## 8. 炸弹渲染

- [ ] 8.1 实现 `drawBombs()` — 休眠炸弹（暗红球体 + 慢呼吸光晕）和不稳定炸弹（橙红闪烁 + 火花粒子）的区分渲染
- [ ] 8.2 不稳定炸弹火花粒子效果（间歇飞溅、颜色来自皮肤参数）

## 9. 爆炸动画

- [ ] 9.1 实现 `drawExplosions()` — 基于时间的 400ms 三阶段动画（Flash/Blast/Decay）
- [ ] 9.2 爆炸粒子飞溅效果（从爆炸中心向四周发射，颜色来自皮肤参数）
- [ ] 9.3 连锁引爆时显示倍率文字（如 "×2"），渐隐消失
- [ ] 9.4 Canvas wrapper 爆炸震屏动画（CSS flash 样式复用或扩展）

## 10. 游戏状态集成

- [ ] 10.1 修改 `draw()` 主函数 — 在食物渲染之后、蛇渲染之前调用 `drawBombs()` 和 `drawExplosions()`
- [ ] 10.2 修改 `gameOver()` — 新增 `reason: 'bomb'` 用于区分炸弹致死，可复用现有 `'collision'` 逻辑
- [ ] 10.3 暂停时炸弹渲染照常（暂停遮罩覆盖在炸弹之上）

## 11. 验证测试

- [ ] 11.1 验证休眠炸弹触碰扣段逻辑（正常扣段 + 蛇过短死亡）
- [ ] 11.2 验证不稳定炸弹触碰引爆 + 蛇头死亡
- [ ] 11.3 验证不稳定炸弹随机自爆
- [ ] 11.4 验证连锁引爆（手动构造 2-3 颗相邻炸弹场景）
- [ ] 11.5 验证护盾防爆炸死亡 + 不防休眠触碰扣段
- [ ] 11.6 验证经典模式特殊道具生成
- [ ] 11.7 验证炸弹生成上限 6 颗
- [ ] 11.8 验证炸弹和食物空间互斥（不在同一格）
- [ ] 11.9 验证 4 套皮肤的炸弹渲染颜色正确
- [ ] 11.10 浏览器中端到端游玩验证炸弹模式体验
