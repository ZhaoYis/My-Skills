# Tasks

## 1. 数据存储层

- [x] 1.1 实现 `LeaderboardStore` 对象：封装排行榜数据的读写、排序、淘汰（最大 20 条）和容量管理逻辑
- [x] 1.2 实现数据迁移函数：首次加载时将 `snake-best-score-classic` / `snake-best-score-timed` 转换为排行榜条目，迁移后清除旧 key
- [x] 1.3 实现昵称管理：读写 `snake-player-name`，默认值 "玩家"，修改时同步更新所有记录的 `playerName` 字段

## 2. UI 组件

- [x] 2.1 新增排行榜完整面板的 HTML 结构（覆盖层、标题栏、筛选按钮、排行表格、昵称编辑区、清空按钮）
- [x] 2.2 新增排行榜面板的 CSS 样式（表格、行高亮、筛选标签、前三名勋章、动画过渡）
- [x] 2.3 新增开始界面排行榜摘要的 HTML 结构和 CSS 样式（Top 5 摘要卡片 + "查看完整榜单"链接）
- [x] 2.4 实现排行表格渲染函数：支持按模式筛选、前三名特殊标识、当前局排名高亮、空数据占位

## 3. 游戏流程集成

- [x] 3.1 修改 `gameOver()` 函数：在游戏结束时调用排行榜存储逻辑（分数 > 0 时保存记录），计算并展示当前排名
- [x] 3.2 修改游戏结束覆盖层：添加排名提示文字和"查看排行榜"按钮
- [x] 3.3 连接排行榜面板事件：打开/关闭面板、筛选切换、昵称修改、清空确认、Escape 键关闭

## 4. 收尾与验证

- [x] 4.1 确保 localStorage 不可用时所有操作静默降级（try/catch 包裹）
- [x] 4.2 手动测试：验证经典/计时模式成绩保存、迁移旧数据、筛选、昵称修改同步、清空确认
- [x] 4.3 验证移动端响应式布局：排行榜面板在窄屏设备上的可用性
