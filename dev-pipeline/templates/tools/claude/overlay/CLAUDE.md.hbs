# CLAUDE.md

This repository was initialized for {{toolName}} via {{packageName}}.

- Skills directory: `{{skillsDir}}`
- Commands directory: `{{commandsDir}}`

## 知识优先（每个开发 / 排查任务开始前）

1. 若存在 `.knowledge/INDEX.md`（或项目既有知识库索引）：先按关键词检索（功能 / 接口 / 数据 / 风险 / 术语）。
   - 命中 → 读取对应知识文件，复用既有结论，避免重复分析。
   - 未命中 → 标记为「知识盲区」，可在后续开发中沉淀知识。
2. 涉及业务「为什么」的判断，遵循 `.knowledge/tech/knowledge-why-policy.md`（遇需求再记，不阻塞当前任务）。
3. 知识写入一律「追加不覆盖」，带日期与来源；归档后可经 `opsx-dev-pipeline` 的归档知识沉淀（决策点 4c）回写。
4. 无 `.knowledge/`（且无其他知识库约定）时本规则自动跳过，不报错。
