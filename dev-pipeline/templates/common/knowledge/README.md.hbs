# {{projectName}} 知识库

这个目录用于沉淀当前项目中可长期复用的知识，而不是一次性的临时分析结果。

## 这个知识库解决什么问题

它主要用于帮助团队成员和 AI 更快回答这些问题：

- 这个项目的核心业务流程是什么？
- 某个接口、规则、配置或外部依赖在哪里定义？
- 某类问题以前踩过什么坑，应该如何规避？
- 某些设计选择背后的 WHY 是什么？

## 目录结构

```text
.knowledge/
├── INDEX.md
├── config/
├── project/
├── business/
├── tech/
│   ├── api/
│   ├── db/
│   ├── development-experience.md
│   ├── todays-mistakes-and-fixes.md
│   └── knowledge-why-policy.md
├── ops/
└── risks/
```

## 适合写进知识库的内容

- 稳定的业务规则、功能闭环与领域概念
- API / 命令入口、数据模型、配置约束、外部依赖说明
- 已验证的排障经验、已知风险、运维操作与发布检查项
- 有长期复用价值的 WHY 背景、FAQ、术语解释
- 对新人 onboarding、后续维护和 AI 协作有帮助的结构化认知

## 不适合直接写进知识库的内容

- 一次性对话结论、没有验证的猜测
- 大段源码复制、临时调试日志
- 只对当前任务短期有效、后续很快失效的中间分析
- 没有来源依据、无法回溯的口头判断

## 写作约定

- 优先补充已有知识，而不是重复新建同主题文档。
- 尽量保留来源指针，便于后续回溯代码和文档。
- 一条知识尽量只表达一个核心认知。
- 若内容仍不确定，明确标记“待确认”。
- 若新增内容涉及 API、功能域、表、外部服务、风险或运维主题，应同步检查 `INDEX.md` 是否需要更新。

## 推荐维护方式

1. 先确认知识应该写到哪个分类目录。
2. 优先补充已有条目，必要时再新建文档。
3. 写入后同步检查 `INDEX.md` 是否需要补索引入口。
4. 定期把散落在对话、注释、故障排查中的稳定认知回收进知识库。
