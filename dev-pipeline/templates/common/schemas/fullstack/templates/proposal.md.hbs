## Why

<!-- Explain the motivation for this change. What problem does this solve? Why now? -->

## What Changes

### Backend Changes
<!-- Describe API, domain, persistence, migration, and integration changes. Define the backend contract that will drive frontend development. -->

### Frontend Changes
<!-- Describe routes, components, interactions, and state changes derived from the backend API contract. -->

## API Contract and Delivery Sequence

<!-- List changed endpoints, request/response DTOs, validation rules, and error codes. Deliver strictly in this order: finalize the contract -> implement and verify the backend -> hand off the API -> implement the frontend -> run integration/E2E verification. Backend and frontend implementation must not run in parallel. -->

## Capabilities

### New Capabilities
<!-- Capabilities being introduced. Each creates specs/<name>/spec.md -->
- `<name>`: <brief description, including backend API scope and frontend consumption scope>

### Modified Capabilities
<!-- Existing capabilities whose REQUIREMENTS are changing (not just implementation).
     Only list here if spec-level behavior changes. -->
- `<existing-name>`: <what requirement is changing>

## Impact

### Backend Impact
<!-- Affected backend modules, APIs, data, dependencies, migrations, and operations -->

### Frontend Impact
<!-- Affected routes, components, state, API clients, dependencies, browsers, and devices -->

### Integration Impact
<!-- Contract compatibility, deployment order, rollback, observability, and end-to-end workflow impact -->
