---
name: opsx-health
description: 知识库健康巡检（对话内）：复用 doctor 检查结果，产出结构化健康报告与分级修复建议，并可驱动修复。
license: MIT
metadata:
  author: zhaoyi
  version: "1.0"
---

# 知识库健康巡检（对话内）

`opsx-health` 在对话内对知识库做一次健康巡检：调用 `opsx-dev-pipeline doctor` 取得检查结果，产出结构化健康报告（评分 / 维度 / 趋势），并按 P0–P3 给出排序后的修复建议，必要时直接驱动修复。

它**复用** `doctor` 的检查逻辑，不重写检查规则；`doctor` 是检查权威，本 skill 是"把结果讲清楚 + 给修复路径 + 可执行修复"的对话内包装。

**重要：** 所有输出使用中文。

## Input

- 当前仓库（含或不含 `.knowledge/` 或项目既有知识目录）
- 可选：用户指定关注的维度（断链 / 重复 / 老化 / 索引等）

## 最小执行约束摘要

- 优先调用 `opsx-dev-pipeline doctor --json` 取结构化结果；不可用时降级到现有存在性检查。
- 报告须包含：总体状态/评分、按维度的问题、按 P0–P3 排序的修复建议。
- 修复属于写操作，**需用户确认后**才执行；不静默修改知识库。
- 不重写 doctor 的检查规则；以其输出为事实来源。

## 阅读顺序

1. 先阅读本页约束摘要与 Phase 引用表
2. 按 Phase 顺序进入 `references/phase-*.md`
3. 生成报告时套用 `assets/report-template.md`
4. 维护本 skill 时，先看 `assets/maintenance-index.md`

## Phase 引用表

| Phase | 说明 | 引用文件 |
| ----- | ---- | -------- |
| 1 | 调用 doctor 取结果 | `references/phase-1-run-doctor.md` |
| 2 | 生成报告 + 修复建议 + 可选执行修复 | `references/phase-2-report-and-fix.md` |
| — | 健康报告模板 | `assets/report-template.md` |
| — | 维护索引 | `assets/maintenance-index.md` |

## 权威来源地图

- **Phase 执行正文**：`references/phase-*.md`
- **健康报告模板**：`assets/report-template.md`
- **维护索引**：`assets/maintenance-index.md`
- **检查事实来源**：`opsx-dev-pipeline doctor`（CLI，不在本 skill 内重写）

## 与其他能力的关系

- 与 `doctor` CLI：互补。CLI 负责检查与评分；本 skill 负责对话内的可读报告、分级修复建议与可选执行。
- 与 `opsx-learn`：修复涉及写入知识库时，遵循 `opsx-learn` 的写入目标 / 去重 / 条目格式约定（存在时引用，不重复造正文）。

## 使用方式

- 调用：`/opsx-health`。
- 默认在对话输出报告 + 分级修复建议；执行修复前先经用户确认。

## 完成标准

- 已取得 doctor 结果（或在不可用时明确说明降级）。
- 输出结构化健康报告与按 P0–P3 排序的修复建议。
- 任何写操作均在用户确认后执行。
