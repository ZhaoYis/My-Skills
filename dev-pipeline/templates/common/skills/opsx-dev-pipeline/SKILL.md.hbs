---
name: opsx-dev-pipeline
description: 执行基于 OpenSpec 和 Git 的门禁式需求开发与交付流程，覆盖预检、提案确认、实现、代码审查、测试、验证归档、提交、推送与合并，并支持中断恢复。用户要求实现或继续一个 OpenSpec change、按阶段推进完整开发流水线、审查并交付变更，或处理 opsx-dev-pipeline 时使用。
allowed-tools: Bash(openspec:*), {{askTool}}
version: "{{~packageVersion~}}"
license: "{{~packageLicense~}}"
repository: "{{~packageRepoUrl~}}"
---

# 需求开发全流程流水线

## Input

用户的需求描述，或一个已有的 change 名称。

## 入口决策树

```text
IF 用户提供了已有 change 名称:
  1. 读取状态: node <SKILL_ROOT>/scripts/dev-pipeline-state.mjs get "<change>"
  2. 从状态读取 currentPhase 和 route
  3. 加载当前 Phase:
     opsx-dev-pipeline load --phase <currentPhase> --change "<change>" --dir "<repo-root>" --format markdown
  4. 按加载结果续接
ELSE IF 用户提供了需求描述:
  1. 加载 Phase 0:
     opsx-dev-pipeline load --phase 0 --change "<change>" --dir "<repo-root>" --format markdown
  2. 完成预检、Route 确认和状态初始化
  3. 按 route 分流: trivial → Phase 2；standard/full → Phase 1
ELSE:
  使用 {{askTool}} 询问：继续已有 change / 创建新 change / 查看 change 列表
```

不得把新 change 固定迁移到 Phase 1。Phase 0 确认 route 后，必须按 Route 矩阵选择下一 Phase。

## Route 矩阵

| Route | 适用场景 | Phase 路径 |
|-------|---------|-----------|
| `trivial` | 无运行时行为变化的 typo、格式、注释或极小配置调整 | 0 → 2 → 6 |
| `standard` | 常规功能、Bug 修复、小范围重构 | 0 → 1 → 2 → 3 → 6 |
| `full` | 核心逻辑、数据库、安全或跨模块高风险变更 | 0 → 1 → 2 → 3 → 4 → 5 → 6 → 7 |

## Phase 加载协议

进入或恢复任何 Phase 前，只使用 CLI 加载完整 bundle：

```bash
opsx-dev-pipeline load --phase <N> --change "<change>" --dir "<repo-root>" --format markdown
```

`load` 会根据 change 状态和 route 合成 Phase 指引与相关 Knowledge；不得再分别实现 Route 判断或 Knowledge 选择。旧集成可以继续调用 `node <SKILL_ROOT>/scripts/load-phase.mjs ...`，该脚本仅兼容转发到同一 CLI。

## 执行约束

- 开始执行前读取 `openspec/config.yaml` 的 `language` 与 `rules.language`；所有 AI 产出物遵循该语言约束。
- 用户补充需求、提案修改或实施说明后，在同一回复中同步当前 Phase、change 和下一动作。
- 除用户明确选择终止或暂停外，不得单方结束全流程。
- 高风险决策必须显式确认；推荐项不等于自动代选。
- 决策点首选 **{{askTool}}** tool；不可用时使用编号选项并等待回复。
- 代码审查修复循环最多 3 轮，超过后强制暂停。
- `openspec/.pipeline-state/<change>.json` 是恢复权威；状态与仓库事实不一致时暂停，不得猜测。

**`<SKILL_ROOT>`**：本技能安装根目录（内含 `scripts/`）。命令在目标 Git 仓库根目录执行。
{{#if skillRootNote~}}
{{{skillRootNote}}}
{{else~}}
- 对于 {{toolName}} 安装：`<SKILL_ROOT>` = `{{skillsDir}}/opsx-dev-pipeline`
{{/if~}}
- 若宿主将 Skill 安装到其他位置，`<SKILL_ROOT>` = 当前 `SKILL.md` 所在目录。
- 引用前先确认目录存在：`test -d "<SKILL_ROOT>/scripts" || echo "scripts not found"`

## 状态协议

- 新 change：Phase 0 取得外部需求关联和 route 的明确选择后，执行 `init "<change>" "<source-branch>" --feature-id "<featureId>" --route "<route>"`（有 URL 时追加 `--feature-url`），或执行 `init "<change>" "<source-branch>" --skip-feature-association --route "<route>"`。
- 读取状态：`node <SKILL_ROOT>/scripts/dev-pipeline-state.mjs get "<change>"`
- 迁移旧 Schema：先运行 `migrate-schema "<change>"`，用户同意后运行 `migrate-schema "<change>" --confirm`。
- 阶段迁移：`node <SKILL_ROOT>/scripts/dev-pipeline-state.mjs transition "<change>" <phase> <step>`
- 记录决策：`node <SKILL_ROOT>/scripts/dev-pipeline-state.mjs decision "<change>" <key> <json-value>`
- 用户确认决策：追加 `--user-confirmed --summary "<确认摘要>"`。
- 记录结果：`node <SKILL_ROOT>/scripts/dev-pipeline-state.mjs set "<change>" <field> <json-value>`
- 记录尝试：`node <SKILL_ROOT>/scripts/dev-pipeline-state.mjs attempt "<change>" <review|tests|verify> <status>`
- 暂停：`node <SKILL_ROOT>/scripts/dev-pipeline-state.mjs pause "<change>" "<reason>"`
- 完成：`node <SKILL_ROOT>/scripts/dev-pipeline-state.mjs complete "<change>"`

状态命令返回 exit code 10/11/12 时，分别按“状态不存在 / 非法迁移或 gate / I/O 或并发冲突”处理。并发冲突只允许重载重试一次。

## 必要门禁

关键转换前执行 `node <SKILL_ROOT>/scripts/gate-check.mjs <subcommand> "<change>"`：

| 子命令 | 时机 | 必须确认 |
|--------|------|---------|
| `pre-apply` | Phase 1 → 2 | `proposalApproved` |
| `pre-review` | Phase 2 → 3 | `implementationConfirmed` |
| `pre-merge` | Phase 6 → 7（仅 full） | `mergeConfirmed` |
| `pre-test-skip` | 测试失败后跳过 | `testSkipConfirmed` |

门禁返回 `confirmation-required` 时必须使用 {{askTool}}；返回 `blocked` 时先修复。违反确认点时暂停流水线。

## 错误处理速查

| 场景 | 处理 |
|------|------|
| `opsx-dev-pipeline` CLI 不可用 | 明确提示安装或修复 PATH 后退出，不回退到本地 Route/Knowledge 实现 |
| OpenSpec CLI 不可用 / 非 Git 仓库 | 提示安装或进入正确仓库后退出 |
| OpenSpec 未初始化 | 提示执行 `openspec init` 后退出 |
| change 不存在 | 列出可用 change 供选择 |
| apply 返回 `blocked` | standard/full 回 Phase 1；trivial 暂停并升级 route 或补齐制品 |
| 修复循环达到 3 轮 | 强制暂停并提示人工介入 |
| 测试、验证、推送或合并失败 | 保留状态，展示事实并等待用户选择；禁止自动破坏性恢复 |
