---
name: opsx-verify
description: 语言无关的通用验证流程：从变更解析验证目标，按项目基准构建/启动，做冒烟/契约/数据校验与影响面回归。
license: MIT
metadata:
  author: zhaoyi
  version: "1.0"
---

# 通用验证流程

`opsx-verify` 用于系统性地验证一次变更：从设计/变更中提取验证目标，按项目基准构建并启动，执行冒烟、契约、数据校验，并对影响面做回归，最后给出明确的通过标准与失败处理回路。

它是"如何系统性验证一次变更"的能力库。所有命令均从项目基准读取，不写死任何具体语言或构建工具。

**重要：** 所有输出使用中文。

## Input

任一验证入口：
- change 名称 / 变更说明
- `opsx-design` 产出的"验证断言字段"（优先复用）
- 受影响的接口 / 数据 / 模块清单
- 项目基准（构建 / 启动 / 测试 / 校验命令来源）

## 最小执行约束摘要

- 构建 / 启动 / 测试 / 校验命令**一律从项目基准解析**（`openspec/config.yaml` 优先，回退 `AGENTS.md` → `CLAUDE.md` → README / 脚本启发式），不写死技术栈。
- 验证目标优先复用 `opsx-design` 的"验证断言字段"，形成 design→verify 链路。
- 区分自动验证与人工验证：纯查询 / 简单入参 → 自动；写操作 / 复杂对象 / 状态流转 → 人工清单。
- 失败时给出修复回路，而不是直接判定结束。
- 本 skill 是"如何验证"的能力库，`opsx-dev-pipeline` Phase 4 仍是"何时必须验证"的门禁权威，避免产生第二份门禁正文。

## 阅读顺序

1. 先阅读本页约束摘要与 Phase 引用表
2. 先执行一次 `scripts/opsx-verify-preflight.sh` 了解项目基准与构建/测试命令的可发现性
3. 按 Phase 顺序进入 `references/phase-*.md`
4. 整理验证目标时套用 `assets/verify-target-template.md`
5. 判定通过/失败时核对 `assets/pass-criteria.md`
6. 维护本 skill 时，先看 `assets/maintenance-index.md`

## Phase 引用表

| Phase | 说明 | 引用文件 |
| ----- | ---- | -------- |
| 1 | 从 change/design 解析验证目标 | `references/phase-1-resolve-context.md` |
| 2 | 构建 + 启动（命令来自项目基准） | `references/phase-2-build-and-run.md` |
| 3 | 冒烟 / 契约 / 数据校验（自动 vs 人工） | `references/phase-3-functional-check.md` |
| 4 | 影响面回归 | `references/phase-4-regression.md` |
| — | 验证目标清单模板 | `assets/verify-target-template.md` |
| — | 通过标准 + 失败处理回路 | `assets/pass-criteria.md` |
| — | 维护索引 | `assets/maintenance-index.md` |

## 权威来源地图

- **Phase 执行正文**：`references/phase-*.md`
- **验证目标清单模板**：`assets/verify-target-template.md`
- **通过标准与失败回路**：`assets/pass-criteria.md`
- **维护索引**：`assets/maintenance-index.md`
- **预检脚本**：`scripts/opsx-verify-preflight.sh`

## 与其他能力的关系

- **上游**：`opsx-design` 的验证断言字段直接喂入本 skill 的验证目标。
- **与 Pipeline**：`opsx-dev-pipeline` Phase 4 的 verify 门禁在需要完整验证时可"加载 opsx-verify 执行"，而非仅跑单条命令；Phase 4 仍是门禁权威。

## 使用方式

- 可独立调用：`/opsx-verify`（或带 change 名）。
- 也可被 Phase 4 在需要完整验证时加载。
- 默认在对话中给出验证步骤与结论；失败时给出修复回路。

## 完成标准

- 已从 change/design 解析出明确的验证目标（接口/数据/影响面）。
- 已按项目基准给出构建/启动/校验步骤。
- 已区分自动与人工验证并给出通过标准。
- 失败时给出修复回路，不绑定任何语言。
