---
name: opsx-learn
description: 面向现有仓库的渐进式知识沉淀流程。
license: MIT
metadata:
  author: zhaoyi
  version: "1.0"
---

# 渐进式知识沉淀

`opsx-learn` 用于把现有仓库中的隐性知识，逐步整理成可复用、可审核、可持续维护的知识条目。

**重要：** 所有输出使用中文。

## Input

用户提供的任一学习入口：
- API 路径
- 方法 / 类 / 模块名
- 功能描述
- 需求文档片段
- 业务域或知识空白点
- 现有知识条目的修订诉求

## 最小执行约束摘要

- 先明确学习目标，再展开追踪；范围模糊时必须先向用户确认。
- 先做预检：确认仓库上下文、现有知识目录、相关文档入口是否可用；若预检返回 `knowledgeHealth`，在落盘前必须先查看并提示其中的健康问题。
- 先分析、后落盘；未经用户确认，不得直接写入知识库。
- 对业务语义不猜测；事实不足时只能标记不确定项并向用户追问。
- 知识沉淀默认采用“通用知识库”模式：优先复用项目现有知识目录；若是首次使用且仓库内无明显知识目录，必须明确提示用户知识库存放位置。通过 `opsx-dev-pipeline init` 初始化的新项目，默认会生成 `.knowledge/` 骨架；若无既有约定，则优先使用该目录。
- 写入时必须先做去重检查，优先追加或合并，不直接覆盖既有内容。
- 输出的知识应尽量与技术栈无关、与仓库结构解耦，但必须保留足够的源码/文档来源指针，便于回溯。
- 优先沉淀长期稳定、可复用、能帮助后续协作的认知，不机械搬运实现细节。

## 阅读顺序

1. 先阅读本页的约束摘要与 Phase 引用表
2. 先执行一次 `scripts/opsx-learn-preflight.sh` 了解仓库是否已有知识库落点，以及是否存在可提前提醒的知识健康问题
3. 再进入当前 Phase 对应的 `references/phase-*.md`
4. 遇到写入位置、去重、条目格式时，查阅 `assets/*.md`
5. 维护本 skill 时，先看 `assets/maintenance-index.md`

## Phase 引用表

| Phase | 说明 | 引用文件 |
| ----- | ---- | -------- |
| 1 | 明确目标与边界 | `references/phase-1-understand-goal.md` |
| 2 | 定位入口与上下文 | `references/phase-2-locate-entry.md` |
| 3 | 追踪实现链路 | `references/phase-3-trace-flow.md` |
| 4 | 提炼知识草稿 | `references/phase-4-draft-knowledge.md` |
| 5 | 审核、确认并沉淀 | `references/phase-5-review-and-write.md` |
| — | 写入位置规则 | `assets/write-targets.md` |
| — | 去重规则 | `assets/dedup-rules.md` |
| — | 知识条目模板 | `assets/knowledge-entry-templates.md` |
| — | 预检输出约定 | `assets/preflight-json-contract.md` |
| — | 维护索引 | `assets/maintenance-index.md` |

## 权威来源地图

- **Phase 执行正文**：`references/phase-*.md`
- **写入位置约定**：`assets/write-targets.md`
- **去重与合并规则**：`assets/dedup-rules.md`
- **知识条目模板**：`assets/knowledge-entry-templates.md`
- **预检输出约定**：`assets/preflight-json-contract.md`
- **维护影响矩阵**：`assets/maintenance-index.md`
- **预检脚本**：`scripts/opsx-learn-preflight.sh`（包含推荐知识目录；若可用，也返回 `knowledgeHealth`、`knowledgeHealthSummary` 与 `knowledgeHealthHighlights` 供落盘前提示）

## 通用适用范围

本 skill 适用于以下类型的知识沉淀：
- 功能闭环与业务流程
- API / 命令 / 任务入口
- 数据模型、配置约束与状态流转
- 外部依赖、三方集成与边界条件
- 排障经验、常见坑、运行约束
- FAQ、术语表与新人入门知识
- 现有知识条目的补充、校正与结构化整理

## 执行说明

- 本 skill 适用于“从代码/文档中提炼可复用知识”的场景，不替代实现开发流程。
- 优先复用项目现有的 README、CLAUDE.md、AGENTS.md、设计文档、ADR、知识目录、注释良好的测试等上下文。
- 若用户只要求分析，不要求落盘，可在 Phase 5 停在“审核完成、暂不写入”。
- 若项目已有知识目录，优先写入既有结构；若没有，首次使用时明确提示用户确认。通过 `opsx-dev-pipeline` 初始化的新项目默认会生成 `.knowledge/` 骨架；若无既有约定，优先使用该目录。
- 若预检返回 `knowledgeHealth`，在真正写入前先向用户提示其中的 `warn` / `fail` 项，尤其是骨架缺失、`INDEX.md` 缺 section 或索引占位符过多等问题。
- 若已有知识条目明显过时，本 skill 应优先给出“补充 / 修订建议”，而不是直接重写全部内容。
- 若上下文不足以得出稳定结论，应输出“已确认事实 / 待确认假设”两部分，避免误导。
- 输出知识时优先选择稳定、可迁移、可复用的表达方式，减少对瞬时实现细节的依赖。

## 推荐主流程

1. 识别输入类型与当前目标。
2. 运行预检并确认知识库存放位置。
3. 找到代码、文档、测试或配置中的实际入口。
4. 顺着调用链、依赖、状态与规则追踪实现。
5. 把事实整理成候选知识条目草稿。
6. 逐条或分组让用户确认：保留 / 修改 / 跳过。
7. 确认知识库位置后，按去重规则追加沉淀。

## 完成标准

- 学习范围明确且得到用户确认。
- 关键实现链路、业务规则、依赖边界已被梳理。
- 输出了结构化知识草稿，并明确标注来源。
- 用户已确认要保留的条目与写入位置。
- 首次使用时已明确知识库存放位置；无既有约定时默认 `.knowledge/`。
- 沉淀结果未覆盖旧知识，且符合去重规则。
