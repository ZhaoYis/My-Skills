# opsx-pr

创建 GitHub Pull Request。由 `opsx-dev-pipeline` Phase 7 在 PR 模式下调用，也可独立使用。

## 使用方式

```
/opsx-pr <change-name>
```

## 推荐流程

1. 读取 skill 定义：`{{skillsDir}}/opsx-pr/SKILL.md`
2. 收集 PR 参数（目标分支、标题、描述、Draft 状态）
3. 执行 `gh pr create`
4. 更新 runtime state
5. 输出 PR URL

## 降级

若 `gh` CLI 不可用，自动生成平台无关 PR 模板，不阻塞流水线。
