# Tasks

## 1. 模式选择 UI
- [ ] 1.1 在 `#startOverlay` 中新增两张模式选择卡片 HTML（经典 / 计时）
- [ ] 1.2 添加模式卡片 CSS 样式（默认态、选中态、悬停态、移动端适配）
- [ ] 1.3 实现模式切换 JS 逻辑（点击切换、键盘←→切换、localStorage 持久化）
- [ ] 1.4 卡片上展示对应模式的最高分，初始从 localStorage 读取

## 2. 游戏状态扩展
- [ ] 2.1 新增状态字段：`gameMode`、`timeRemaining`、`maxTime`、`foodItem`、`shieldActive`、`boostActive`、`boostTimer`
- [ ] 2.2 修改 `initGame()` 根据 `gameMode` 初始化不同状态
- [ ] 2.3 修改 `startGame()` 读取当前选中的模式

## 3. 计时器系统
- [ ] 3.1 在 Header 新增"剩余时间"卡片 HTML
- [ ] 3.2 添加计时器卡片 CSS（绿/黄/红颜色状态、红色脉冲动画 `@keyframes`）
- [ ] 3.3 在 `tick()` 中新增秒级 accumulator 实现倒计时递减
- [ ] 3.4 计时器与暂停联动（暂停时停止计时）
- [ ] 3.5 时间归零触发 `gameOver(false, 'timeUp')`

## 4. 特殊物品系统
- [ ] 4.1 定义物品类型枚举和概率表
- [ ] 4.2 修改 `spawnFood()` 按概率生成不同物品类型
- [ ] 4.3 金苹果 Canvas 绘制（金色球体 + 粒子旋转光）
- [ ] 4.4 闪电 Canvas 绘制（蓝色电光球 + 电弧闪烁）
- [ ] 4.5 护盾 Canvas 绘制（紫色光环球 + 光环脉动）
- [ ] 4.6 修改食物碰撞逻辑：根据 `foodItem` 类型分发不同效果

## 5. 护盾机制
- [ ] 5.1 实现护盾状态管理（激活、消耗、指示器）
- [ ] 5.2 修改碰撞检测：护盾激活时方向反转而非游戏结束
- [ ] 5.3 护盾消耗视觉反馈（屏幕短暂闪光）

## 6. 闪电机制
- [ ] 6.1 实现闪电速度减半效果
- [ ] 6.2 实现 3 秒计时器自动恢复速度
- [ ] 6.3 蛇身蓝色描边视觉反馈

## 7. 分数存储隔离
- [ ] 7.1 修改 localStorage 键名：经典模式用 `snake-best-score-classic`，计时模式用 `snake-best-score-timed`
- [ ] 7.2 迁移旧键 `snake-best-score` → `snake-best-score-classic`（一次性）
- [ ] 7.3 修改 `gameOver()` 根据模式写入对应键

## 8. 游戏结束区分
- [ ] 8.1 修改 `gameOver()` 接受结束原因参数（`'collision'` / `'timeUp'` / `'win'`）
- [ ] 8.2 "时间到"覆盖层展示不同标题和颜色
- [ ] 8.3 经典模式下计时器卡片隐藏（或显示为得分卡片保持布局一致）

## 9. 整体集成与回归验证
- [ ] 9.1 经典模式行为与改动前完全一致（回归验证）
- [ ] 9.2 计时模式完整流程可玩（选择模式 → 开始 → 吃食物 → 时间到/碰撞结束）
- [ ] 9.3 移动端适配验证（模式卡片、计时器卡片、特殊物品可见性）
- [ ] 9.4 皮肤切换在两种模式下均正常工作
