---
description: opsx-dev-pipeline 知识优先与流水线入口规则
globs:
alwaysApply: false
---

# opsx-dev-pipeline / Cursor Rule

This project uses Cursor rules plus repository-local prompt assets.

- Rules directory: `.cursor/rules`
- Commands directory: `.cursor/commands`
- Skills-style assets are stored in: `.cursor/rules`
- Load `.cursor/rules/opsx-dev-pipeline/SKILL.md` only when the user requests an OpenSpec change or the gated delivery pipeline.
