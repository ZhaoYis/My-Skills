---
name: opsx-init
description: 分析当前项目并初始化或更新 openspec/config.yaml 文件。用户要求初始化 config、设置项目上下文、或更新 OpenSpec 配置时使用。
allowed-tools: Bash, {{askTool}}
version: "{{~packageVersion~}}"
license: "{{~packageLicense~}}"
repository: "{{~packageRepoUrl~}}"
---

# opsx-init

分析当前项目上下文，自动生成或更新 `openspec/config.yaml`。

## 执行约束

- 所有产出物必须遵循项目现有的语言约定。
- 生成 config.yaml 前，必须检查项目文件系统，不得凭空猜测。
- 如果 config.yaml 已存在，必须展示差异建议并使用 `{{askTool}}` 确认后再更新。
- 如果 config.yaml 不存在，直接创建，无需确认。
- 不得修改 config.yaml 之外的任何文件。

## 工作流

### 1. 检测项目上下文

按以下顺序检测项目信息，每一步记录检测结果：

#### 1.1 检测编程语言和技术栈

| 检测条件 | 结果 |
|---------|------|
| 存在 `tsconfig.json` | TypeScript |
| 存在 `package.json` 且含 TypeScript 依赖 | TypeScript |
| 存在 `pyproject.toml` | Python |
| 存在 `requirements.txt` | Python |
| 存在 `pom.xml` | Java (Maven) |
| 存在 `build.gradle` 或 `build.gradle.kts` | Java (Gradle) |
| 存在 `go.mod` | Go |

从 `package.json` 中提取：
- `name`、`description` → 项目名称和描述
- `dependencies` / `devDependencies` → 关键依赖（框架、测试库等）
- `scripts` → 常用命令（build、test、lint）

#### 1.2 检测 AI 工具

| 检测条件 | 结果 |
|---------|------|
| 存在 `.claude/` 目录 | Claude Code |
| 存在 `.cursor/` 目录 | Cursor |
| 存在 `.agents/` 目录 | Codex / OpenCode |
| 存在 `.opencode/` 目录 | OpenCode |

#### 1.3 检测项目类型

| 检测条件 | 结果 |
|---------|------|
| 存在 `src/cli/` 或 `bin/` 目录 | CLI Tool |
| 存在 `src/server/` 或 web 框架依赖 | Web Application |
| 存在 `src/lib/` 或 `index.ts` 导出 | Library |
| 存在 `pages/` 或 `app/` 目录（Next.js） | Web Application |

#### 1.4 检测已有 OpenSpec 配置

- 读取 `openspec/config.yaml`（如果存在）获取已有的 `schema` 和 `language` 配置
- 如果 `openspec/` 目录不存在，在步骤 3 中创建

### 2. 汇总检测结果

整理检测到的信息：
- 技术栈（TypeScript/Node.js, Python, Java 等）
- 项目类型（CLI tool, Web Application, Library 等）
- 领域（从项目名称和描述推断）
- AI 工具列表
- 关键组件和目录结构
- 代码约定（从工具配置推断：Biome/ESLint, pytest, Ruff 等）

### 3. 读取已有的 config.yaml 作为参考模板

**必须先读取系统中已有的 `openspec/config.yaml` 文件**，以其格式和结构为模板。

如果 `openspec/config.yaml` 不存在，使用 `{{askTool}}` 询问用户期望的 schema 类型（默认 `spec-driven`），然后以当前 dev-pipeline 自带的 `openspec/config.yaml` 作为参考模板。

### 4. 填充 config.yaml

以步骤 3 读取到的 config.yaml 结构为模板，将步骤 2 检测到的项目信息填入相应字段：

- **`context` 字段**：将检测到的技术栈、项目类型、领域、约定、关键组件、AI 工具等信息更新到 `context` 中
- **`rules` 字段**：保留已有 rules 结构不变，仅根据检测结果调整：
  - 根据检测到的语言调整 `specs` 规则中的 `<language>`（TypeScript/Python/Java 等）
  - 如果检测到 Python，将 JSDoc 相关规则改为 Google-style docstrings
  - 根据项目类型调整 `proposal` 规则中的关注点
  - 根据检测到的包管理器调整 `operations` 中的 build/test 命令路径
- **其他字段**：保留 `schema`、`language` 等已有字段不变，除非检测到明确的变化

**核心原则**：以已有 config.yaml 为模板，只填充和更新检测到的项目信息，不改变其结构和格式。

### 5. 写入文件

- 若 `openspec/config.yaml` 不存在：以步骤 3 读取的参考模板为基础，填充检测到的项目信息后创建，报告路径
- 若 `openspec/config.yaml` 已存在：展示差异（新增/修改/删除的字段），使用 `{{askTool}}` 让用户选择：
  - "更新（Update）" — 仅更新检测到的变化字段（如 context、operations 命令），保留用户自定义的 rules
  - "覆盖（Replace）" — 用参考模板重新填充所有内容
  - "取消（Cancel）" — 不做任何修改

### 6. 输出报告

完成后报告：
- 文件路径
- 检测到的技术栈、语言、AI 工具
- 所做更改的摘要
- 任何未确定的 `待确认` 项

## Guardrails

- 仅修改 `openspec/config.yaml`，不得修改其他文件
- 不创建 `openspec/` 之外的目录
- 如果检测到矛盾信息（如同时存在 Python 和 Java 配置），报告检测结果并使用 `{{askTool}}` 确认
- 不从空项目推断信息，缺失信息时使用 `{{askTool}}` 询问
- 不得凭空编造依赖、框架或项目类型