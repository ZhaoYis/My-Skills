---
{{#if (isTool "cursor")}}
name: /opsx-dev-pipeline
id: opsx-dev-pipeline
{{else}}
name: opsx:dev-pipeline
{{/if}}
description: Drive the full OpenSpec + Git development pipeline (Phase 0-7)
{{#if (isTool "claude")}}
allowed-tools: Bash(openspec:*), {{askTool}}
category: Workflow
tags: [workflow, experimental]
{{else}}{{#if (isTool "cursor")}}
category: Workflow
{{/if}}{{/if}}
---

# opsx-dev-pipeline

Use this command as the entrypoint for the bundled `opsx-dev-pipeline` skill.

## Intent

Drive the full OpenSpec + Git development pipeline through the installed skill at:

- `{{skillsDir}}/opsx-dev-pipeline/SKILL.md`

## Flow

Load and follow the `opsx-dev-pipeline` skill. It covers Phase0–7: preflight → propose → apply → review → unit tests → archive → commit and push → optional merge delivery.
