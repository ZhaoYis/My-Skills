# OpenSpec custom schema - Backend only
schema: custom
stacks:
  - backend

context: |
  Backend-only Todo API built with Express and TypeScript.

rules:
  verify: ./scripts/validate.sh backend
  test: npm test --workspace=backend
  build: cd backend && npm install && npm run build
