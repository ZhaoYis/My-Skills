{
  "name": "opsx-dev-pipeline-delivery-tests",
  "private": true,
  "version": "1.0.0",
  "description": "Deterministic end-to-end delivery tests for the installed opsx-dev-pipeline skill",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:report": "vitest run --reporter=verbose"
  },
  "dependencies": {
    "fs-extra": "^11.3.2",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/fs-extra": "^11.0.4",
    "@types/node": "^24.0.1",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4",
    "opsx-dev-pipeline": "file:.."
  }
}
